# PickPlay Backend — Environment Variables
# ─────────────────────────────────────────
# Copy this file, rename it to: .env
# Then fill in your values from Supabase.
# NEVER share this file publicly.

# 1. Your Supabase project URL
#    Where to find it: supabase.com → your project → Settings → API → Project URL
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co

# 2. Your Supabase service role key (NOT the anon key — use the service_role key)
#    Where to find it: supabase.com → your project → Settings → API → service_role
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_SERVICE_KEY_HERE

# 3. A secret password used to sign login tokens (make it long and random)
JWT_SECRET=PickPlay_Super_Secret_2026_Change_This

# 4. Port (Railway sets this automatically, leave as 3001 for local testing)
PORT=3001
